# React TODO Application

A simple and responsive Task Management (TODO) application built with React.js. This app allows users to create, view, and delete tasks.

## 🚀 Features
*   **Add Tasks:** Users can input new tasks via a text field and submit them.
*   **Delete Tasks:** Each task has a delete button to remove it from the list.
*   **Dynamic UI:** List updates instantly without refreshing the page.
*   **Empty State Handling:** Gracefully handles the display when no tasks are present.

## 🛠️ Built With
*   React.js
*   Vite
*   CSS for styling

## 📥 Installation & Setup

1.  **Clone the repository:**
    ```bash
    git clone github.com
    cd todo-app
    ```

2.  **Install dependencies:**
    ```bash
    npm install
    ```

3.  **Run the application:**
    ```bash
    npm run dev
    ```

4.  Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

## 🖥️ Usage
1.  Type your task in the input field.
2.  Click the "Add" button or press Enter to add the task to the list.
3.  Click the "Delete" button next to a task to remove it.


